# Настройка админ-панели

## ✅ Что создано:

1. ✅ Backend API для админ-панели (`/api/admin/*`)
2. ✅ Веб-интерфейс админ-панели (`/admin`)
3. ✅ Аутентификация админа
4. ✅ Управление пользователями, чатами, сообщениями
5. ✅ Статистика и дашборд

---

## 🚀 Развертывание на сервере:

### 1. Загрузите обновленный backend:

```powershell
cd C:\Users\1\Documents\GitHub\kyteMobail
scp -i "C:\Users\1\.ssh\yandex_key\ssh-key-1765132631789" -r backend kyte-777@94.131.88.135:/tmp/
```

### 2. На сервере обновите файлы:

```bash
# Подключитесь к серверу
ssh -i "C:\Users\1\.ssh\yandex_key\ssh-key-1765132631789" kyte-777@94.131.88.135

# Переместите файлы
sudo rm -rf /var/www/kyte-backend/backend/*
sudo mv /tmp/backend/* /var/www/kyte-backend/backend/

# Перейдите в директорию
cd /var/www/kyte-backend/backend

# Установите зависимости (если нужно)
sudo npm install --production

# Обновите .env (добавьте админские учетные данные)
sudo nano .env
```

Добавьте в `.env`:

```env
ADMIN_EMAIL=admin@kyte.com
ADMIN_PASSWORD=admin123
```

**⚠️ ВАЖНО:** Измените пароль на более безопасный!

### 3. Перезапустите backend:

```bash
sudo pm2 restart kyte-backend
sudo pm2 logs kyte-backend --lines 50
```

---

## 🌐 Доступ к админ-панели:

Откройте в браузере:

**http://94.131.88.135/admin**

### Учетные данные по умолчанию:

- **Email:** `admin@kyte.com`
- **Пароль:** `admin123`

---

## 📋 Функционал админ-панели:

### 📊 Дашборд
- Общая статистика (пользователи, чаты, сообщения)
- Статистика за сегодня
- Последние зарегистрированные пользователи
- Самые активные чаты

### 👥 Пользователи
- Просмотр всех пользователей с пагинацией
- Поиск по email, имени, телефону
- Просмотр деталей пользователя
- Удаление пользователей

### 💬 Чаты
- Просмотр всех чатов (личные и группы)
- Фильтрация по типу (личные/группы)
- Просмотр участников чата
- Удаление чатов (вместе с сообщениями)

### 📨 Сообщения
- Просмотр всех сообщений
- Фильтрация по ID чата
- Удаление сообщений

---

## 🔒 Безопасность:

1. **Измените пароль по умолчанию** в `.env` файле
2. Используйте сильный пароль (минимум 12 символов)
3. Токены админа хранятся в localStorage браузера
4. Токены действительны 24 часа
5. Все API запросы требуют Bearer token

---

## 🐛 Отладка:

### Если админ-панель не открывается:

1. Проверьте что backend запущен:
   ```bash
   sudo pm2 status
   ```

2. Проверьте логи:
   ```bash
   sudo pm2 logs kyte-backend --lines 50
   ```

3. Проверьте что файлы админ-панели на месте:
   ```bash
   ls -la /var/www/kyte-backend/backend/admin/
   ```

4. Проверьте доступность:
   ```bash
   curl http://localhost:3000/admin/
   ```

### Если не работает логин:

1. Проверьте `.env` файл:
   ```bash
   cat /var/www/kyte-backend/backend/.env | grep ADMIN
   ```

2. Проверьте API напрямую:
   ```bash
   curl -X POST http://localhost:3000/api/admin/login \
     -H "Content-Type: application/json" \
     -d '{"email":"admin@kyte.com","password":"admin123"}'
   ```

---

## 📚 Документация:

Подробная документация в файле `backend/ADMIN_PANEL.md`

---

## ✅ Готово!

Админ-панель готова к использованию!

