# Админ-панель

## Доступ

Админ-панель доступна по адресу: **http://94.131.88.135/admin**

## Учетные данные по умолчанию

- **Email:** `admin@kyte.com`
- **Пароль:** `admin123`

⚠️ **Важно:** Измените пароль в production!

## Настройка учетных данных

Отредактируйте файл `.env` на сервере:

```bash
sudo nano /var/www/kyte-backend/backend/.env
```

Добавьте или измените:

```env
ADMIN_EMAIL=your-admin-email@example.com
ADMIN_PASSWORD=your-secure-password
```

После изменения перезапустите backend:

```bash
sudo pm2 restart kyte-backend
```

## Функционал

### 📊 Дашборд
- Общая статистика (пользователи, чаты, сообщения)
- Статистика за сегодня
- Последние зарегистрированные пользователи
- Самые активные чаты

### 👥 Пользователи
- Просмотр всех пользователей
- Поиск по email, имени, телефону
- Просмотр деталей пользователя
- Удаление пользователей

### 💬 Чаты
- Просмотр всех чатов (личные и группы)
- Фильтрация по типу
- Просмотр участников чата
- Удаление чатов (вместе с сообщениями)

### 📨 Сообщения
- Просмотр всех сообщений
- Фильтрация по чату
- Удаление сообщений

## API Endpoints

Все endpoints требуют аутентификации через Bearer token.

### Аутентификация
- `POST /api/admin/login` - Вход в админ-панель
- `POST /api/admin/logout` - Выход

### Статистика
- `GET /api/admin/stats` - Получить статистику

### Пользователи
- `GET /api/admin/users` - Список пользователей
- `GET /api/admin/users/:id` - Детали пользователя
- `PUT /api/admin/users/:id` - Обновить пользователя
- `DELETE /api/admin/users/:id` - Удалить пользователя

### Чаты
- `GET /api/admin/chats` - Список чатов
- `GET /api/admin/chats/:id` - Детали чата
- `DELETE /api/admin/chats/:id` - Удалить чат

### Сообщения
- `GET /api/admin/messages` - Список сообщений
- `GET /api/admin/messages/:id` - Детали сообщения
- `DELETE /api/admin/messages/:id` - Удалить сообщение

## Безопасность

1. **Измените пароль по умолчанию** в production
2. Используйте сильный пароль (минимум 12 символов)
3. Токены админа хранятся в localStorage браузера
4. Токены действительны 24 часа
5. Все запросы требуют Bearer token в заголовке Authorization

## Разработка

Админ-панель находится в `backend/admin/`:
- `index.html` - HTML структура
- `styles.css` - Стили
- `app.js` - JavaScript логика

Статические файлы раздаются через Express по пути `/admin`.

