# Остановка всех процессов и перезапуск

## Проблема
Множественные процессы занимают порт 3000, и переменные окружения не загружаются правильно.

## Решение

### 1. Остановить ВСЕ процессы Node.js

```bash
# Найти все процессы node
ps aux | grep node

# Остановить все процессы (кроме grep)
sudo killall node
# или
sudo pkill -f node

# Проверить что порт свободен
sudo lsof -i :3000
```

### 2. Очистить PM2

```bash
pm2 stop all
pm2 delete all
pm2 kill
```

### 3. Перезапустить PM2

```bash
cd /var/www/kyte-backend/backend
pm2 start src/server.js --name kyte-backend
pm2 save
pm2 startup
# Выполните команду которую покажет PM2
```

### 4. Проверить логи

```bash
pm2 logs kyte-backend --lines 30 --nostream
```

Должно быть:
```
📱 SMS Service: Используется AWS SNS для отправки SMS.
```

Вместо:
```
📱 SMS Service: Используется мок-режим.
```

### 5. Протестировать отправку SMS

```bash
curl -X POST http://94.131.88.135/api/auth/phone/send-code \
  -H 'Content-Type: application/json' \
  -d '{"phone": "+79686288842"}'
```

### 6. Найти код в логах

```bash
# Если AWS режим
pm2 logs kyte-backend --lines 50 --nostream | grep -i "aws\|sms\|messageid\|✅"

# Если все еще мок-режим
pm2 logs kyte-backend --lines 200 --nostream | grep -A 3 "MOCK SMS\|код подтверждения"
```



