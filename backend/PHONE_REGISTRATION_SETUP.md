# Настройка регистрации по номеру телефона

## Что реализовано

✅ Модель `PhoneVerification` для хранения SMS кодов  
✅ SMS сервис с поддержкой разных провайдеров  
✅ Backend эндпоинты для отправки и проверки кода  
✅ Frontend UI для регистрации по телефону  
✅ Валидация номера телефона  

## Backend эндпоинты

### POST `/api/auth/phone/send-code`
Отправляет SMS код на указанный номер телефона.

**Тело запроса:**
```json
{
  "phone": "+79991234567"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Код отправлен",
  "expiresIn": 600,
  "code": "123456"  // Только в мок-режиме или development
}
```

### POST `/api/auth/phone/verify-code`
Проверяет код и регистрирует/авторизует пользователя.

**Тело запроса:**
```json
{
  "phone": "+79991234567",
  "code": "123456",
  "name": "Иван Иванов"  // необязательно
}
```

**Ответ:**
```json
{
  "user": {
    "id": "...",
    "email": "phone_79991234567@temp.kyte.me",
    "phone": "+79991234567",
    "name": "Иван Иванов"
  },
  "accessToken": "...",
  "refreshToken": "..."
}
```

## Настройка SMS провайдера

По умолчанию используется **мок-режим** (SMS не отправляются реально, код выводится в консоль).

### Вариант 1: Twilio

1. Зарегистрируйтесь на [Twilio](https://www.twilio.com/)
2. Получите `Account SID`, `Auth Token` и номер телефона
3. Установите пакет:
```bash
npm install twilio
```
4. Добавьте в `.env`:
```env
SMS_PROVIDER=twilio
TWILIO_ACCOUNT_SID=your_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_PHONE_NUMBER=+1234567890
```

### Вариант 2: AWS SNS

1. Настройте AWS аккаунт и получите ключи доступа
2. Установите пакет:
```bash
npm install aws-sdk
```
3. Добавьте в `.env`:
```env
SMS_PROVIDER=aws
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=us-east-1
```

### Вариант 3: Sms.ru

1. Зарегистрируйтесь на [Sms.ru](https://sms.ru/)
2. Получите API ID
3. Установите пакет (если еще не установлен):
```bash
npm install axios
```
4. Добавьте в `.env`:
```env
SMS_PROVIDER=smsru
SMSRU_API_ID=your_api_id
```

### Вариант 4: Мок-режим (для разработки)

По умолчанию используется мок-режим. SMS не отправляются, код выводится в консоль сервера.

```env
SMS_PROVIDER=mock
# или просто не указывайте SMS_PROVIDER
```

## Особенности

- **Валидация номера**: Автоматическая нормализация формата (+7XXXXXXXXXX)
- **Лимиты**: Не более 3 кодов в час на один номер
- **Время жизни кода**: 10 минут
- **Максимум попыток**: 5 попыток ввода кода
- **Автоудаление**: Коды автоматически удаляются после истечения срока действия

## Тестирование

В мок-режиме код отправки SMS выводится в консоль сервера:
```
📱 [MOCK SMS] Отправка на +79991234567: Ваш код подтверждения: 123456. Не сообщайте его никому.
```

В development режиме код также возвращается в ответе API для удобства тестирования.

## Безопасность

- Коды хранятся в базе данных с автоматическим удалением после истечения
- Использованные коды помечаются как `verified` и не могут быть использованы повторно
- Лимит попыток предотвращает брутфорс атаки
- Rate limiting на уровне API предотвращает спам запросов



