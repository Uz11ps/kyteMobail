# Тестирование регистрации по телефону

## ✅ Эндпоинт работает!

Эндпоинт `/api/auth/phone/send-code` успешно отвечает:
```json
{"success":true,"message":"Код отправлен","expiresIn":600}
```

## Проверка режима SMS

### 1. Проверить логи сервера

```bash
# Проверить логи PM2
pm2 logs kyte-backend --lines 50

# Или если используется systemd
sudo journalctl -u kyte-backend -n 50
```

**Если используется мок-режим**, в логах будет:
```
📱 [MOCK SMS] Отправка на +79686288842: Ваш код подтверждения: 123456. Не сообщайте его никому.
```

**Если используется AWS SNS**, в логах будет:
```
✅ SMS отправлено через AWS SNS. MessageId: ..., Phone: +79686288842
```

### 2. Проверить настройки в .env

```bash
cd /var/www/kyte-backend/backend  # или где находится backend
cat .env | grep SMS
```

Должно быть:
```env
SMS_PROVIDER=aws
AWS_ACCESS_KEY_ID=AKIA5GBWTJIVKHZQ2SDN
AWS_SECRET_ACCESS_KEY=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
AWS_REGION=us-east-1
```

## Тестирование полного цикла

### Шаг 1: Отправка кода (уже работает ✅)

```bash
curl -X POST http://94.131.80.213/api/auth/phone/send-code \
  -H "Content-Type: application/json" \
  -d '{"phone": "+79686288842"}'
```

### Шаг 2: Получить код из логов (если мок-режим)

Если используется мок-режим, код будет в логах. Если AWS SNS - код придет на телефон.

### Шаг 3: Проверка кода и регистрация

```bash
curl -X POST http://94.131.80.213/api/auth/phone/verify-code \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+79686288842",
    "code": "123456",
    "name": "Тестовый Пользователь"
  }'
```

**Ожидаемый ответ:**
```json
{
  "user": {
    "id": "...",
    "email": "phone_79686288842@temp.kyte.me",
    "phone": "+79686288842",
    "name": "Тестовый Пользователь"
  },
  "accessToken": "...",
  "refreshToken": "..."
}
```

## Проверка в базе данных

```bash
# Подключиться к MongoDB (если есть доступ)
# Или через MongoDB Compass/Atlas

# Проверить созданного пользователя
db.users.findOne({phone: "+79686288842"})

# Проверить код верификации
db.phoneverifications.findOne({phone: "+79686288842"})
```

## Возможные проблемы

### Проблема: Код не приходит на телефон

**Решение:**
1. Проверить логи - используется ли AWS SNS или мок-режим
2. Если мок-режим - код в логах сервера
3. Если AWS SNS - проверить:
   - Правильность ключей в .env
   - Выход из Sandbox режима в AWS Console
   - Поддержку России/Казахстана в AWS SNS

### Проблема: "Cannot POST /api/auth/phone/verify-code"

**Решение:**
1. Проверить что код обновлен на сервере
2. Перезапустить сервер: `pm2 restart kyte-backend`
3. Проверить логи на ошибки

### Проблема: "Код не найден" или "Код истек"

**Решение:**
1. Код действителен 10 минут
2. Запросить новый код
3. Проверить время на сервере: `date`

## Следующие шаги

1. ✅ Эндпоинт отправки кода работает
2. ⏳ Протестировать получение кода (из логов или на телефон)
3. ⏳ Протестировать верификацию кода и регистрацию
4. ⏳ Протестировать вход по телефону для существующего пользователя



