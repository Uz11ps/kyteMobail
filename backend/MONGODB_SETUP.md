# Настройка MongoDB

## Вариант 1: MongoDB Atlas (Рекомендуется для начала)

MongoDB Atlas - это бесплатный облачный сервис MongoDB. Не требует локальной установки.

### Шаги:

1. **Создайте аккаунт:**
   - Перейдите на https://www.mongodb.com/cloud/atlas/register
   - Зарегистрируйтесь (можно через Google/GitHub)

2. **Создайте бесплатный кластер:**
   - После регистрации нажмите "Build a Database"
   - Выберите бесплатный план "M0 FREE"
   - Выберите регион (ближайший к вам)
   - Нажмите "Create"

3. **Настройте доступ:**
   - В разделе "Network Access" добавьте IP адрес `0.0.0.0/0` (разрешить доступ откуда угодно)
   - Или добавьте ваш текущий IP адрес

4. **Создайте пользователя базы данных:**
   - В разделе "Database Access" нажмите "Add New Database User"
   - Выберите "Password" метод аутентификации
   - Введите имя пользователя и пароль (запомните их!)
   - Роль: "Atlas admin" или "Read and write to any database"
   - Нажмите "Add User"

5. **Получите Connection String:**
   - В разделе "Database" нажмите "Connect"
   - Выберите "Connect your application"
   - Скопируйте connection string (выглядит как: `mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority`)

6. **Обновите .env файл:**
   ```env
   MONGODB_URI=mongodb+srv://ваш-username:ваш-пароль@cluster0.xxxxx.mongodb.net/kyte_chat?retryWrites=true&w=majority
   ```
   
   Замените:
   - `ваш-username` на имя пользователя из шага 4
   - `ваш-пароль` на пароль из шага 4
   - `cluster0.xxxxx` на ваш реальный кластер

**Готово!** MongoDB Atlas настроен и готов к использованию.

---

## Вариант 2: Локальная установка MongoDB

### Windows:

1. **Скачайте MongoDB Community Server:**
   - Перейдите на https://www.mongodb.com/try/download/community
   - Выберите:
     - Version: Latest (7.0+)
     - Platform: Windows
     - Package: MSI
   - Нажмите "Download"

2. **Установите MongoDB:**
   - Запустите скачанный MSI файл
   - Выберите "Complete" установку
   - Установите как Windows Service (рекомендуется)
   - Установите MongoDB Compass (GUI инструмент) - опционально

3. **Проверьте установку:**
   - Откройте PowerShell или CMD
   - Выполните:
     ```powershell
     mongod --version
     ```
   - Должна отобразиться версия MongoDB

4. **Запустите MongoDB:**
   - Если установлен как сервис - он запустится автоматически
   - Если нет, запустите вручную:
     ```powershell
     mongod
     ```
   - MongoDB будет доступен на `mongodb://localhost:27017`

5. **Обновите .env файл:**
   ```env
   MONGODB_URI=mongodb://localhost:27017/kyte_chat
   ```

### Альтернатива: MongoDB через Docker

Если у вас установлен Docker:

```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

Затем в `.env`:
```env
MONGODB_URI=mongodb://localhost:27017/kyte_chat
```

---

## Проверка подключения

После настройки MongoDB, запустите backend:

```bash
cd backend
npm run dev
```

Вы должны увидеть:
```
✅ Подключено к MongoDB
🚀 Сервер запущен на порту 3000
```

Если видите ошибку подключения:
- Проверьте правильность MONGODB_URI в `.env`
- Убедитесь что MongoDB запущен (для локальной установки)
- Проверьте настройки сети в MongoDB Atlas (для облачного варианта)

