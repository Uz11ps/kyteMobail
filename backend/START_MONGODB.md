# 🚀 Быстрый старт MongoDB

## Рекомендуемый способ: MongoDB Atlas (5 минут, БЕСПЛАТНО)

### Пошаговая инструкция:

1. **Откройте:** https://www.mongodb.com/cloud/atlas/register
2. **Зарегистрируйтесь** (можно через Google/GitHub)
3. **Создайте кластер:**
   - Нажмите "Build a Database"
   - Выберите **FREE** план (M0)
   - Выберите любой регион
   - Нажмите "Create" (создание займет 1-2 минуты)

4. **Настройте доступ:**
   - Перейдите в "Network Access" → "Add IP Address"
   - Выберите "Allow Access from Anywhere" (0.0.0.0/0)
   - Нажмите "Confirm"

5. **Создайте пользователя:**
   - Перейдите в "Database Access" → "Add New Database User"
   - Username: `kyteuser` (или любое другое)
   - Password: придумайте и **запомните** пароль
   - Role: "Atlas admin"
   - Нажмите "Add User"

6. **Получите Connection String:**
   - Перейдите в "Database" → "Connect"
   - Выберите "Connect your application"
   - Скопируйте строку подключения

   Пример:
   ```
   mongodb+srv://kyteuser:<password>@cluster0.abc123.mongodb.net/?retryWrites=true&w=majority
   ```

7. **Обновите файл `backend/.env`:**
   
   Откройте `backend/.env` и замените строку:
   ```env
   MONGODB_URI=mongodb://localhost:27017/kyte_chat
   ```
   
   На:
   ```env
   MONGODB_URI=mongodb+srv://kyteuser:ВАШ_ПАРОЛЬ@cluster0.abc123.mongodb.net/kyte_chat?retryWrites=true&w=majority
   ```
   
   **Важно:**
   - Замените `<password>` на ваш реальный пароль
   - Замените `cluster0.abc123` на ваш реальный кластер
   - Добавьте `/kyte_chat` перед `?` для указания базы данных

### ✅ Готово!

Теперь запустите backend:
```bash
cd backend
npm install
npm run dev
```

Вы должны увидеть:
```
✅ Подключено к MongoDB
🚀 Сервер запущен на порту 3000
```

---

## Альтернатива: Локальная установка MongoDB

Если хотите использовать локальный MongoDB:

### Windows:

1. **Скачайте MongoDB:**
   - https://www.mongodb.com/try/download/community
   - Выберите: Windows, MSI, Latest version
   - Нажмите "Download"

2. **Установите:**
   - Запустите скачанный MSI файл
   - Выберите "Complete" установку
   - ✅ Отметьте "Install MongoDB as a Service"
   - ✅ Отметьте "Install MongoDB Compass" (GUI инструмент)
   - Нажмите "Install"

3. **Проверьте:**
   - MongoDB запустится автоматически как Windows Service
   - Откройте MongoDB Compass для проверки

4. **В файле `backend/.env` уже указано:**
   ```env
   MONGODB_URI=mongodb://localhost:27017/kyte_chat
   ```

### Проверка локальной установки:

```powershell
# Проверка версии
mongod --version

# Если команда не найдена, MongoDB не в PATH
# Используйте MongoDB Atlas или добавьте в PATH
```

---

## Помощь

Если возникли проблемы:

1. **Проверьте `.env` файл** - правильный ли MONGODB_URI?
2. **Для Atlas:** Проверьте Network Access (должен быть разрешен доступ)
3. **Для локального:** Убедитесь что MongoDB запущен (Windows Service)
4. **Логи:** Смотрите вывод `npm run dev` для ошибок подключения

