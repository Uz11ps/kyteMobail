# Проверка статуса SMS и тестирование

## Текущая ситуация
- ✅ Эндпоинт `/api/auth/phone/send-code` работает
- ⏳ Нужно проверить логи для получения кода
- ⏳ Нужно протестировать верификацию кода

## Команды для выполнения

### 1. Проверить настройки SMS в .env

```bash
# Вы уже в правильной директории
cat .env | grep -E "SMS|AWS"
```

### 2. Проверить логи PM2 (без прерывания)

```bash
# Посмотреть последние 50 строк логов
pm2 logs kyte-backend --lines 50 --nostream

# Или найти только SMS/Phone/AWS логи
pm2 logs kyte-backend --lines 200 --nostream | grep -i "sms\|phone\|aws\|mock"
```

### 3. Проверить статус PM2 процесса

```bash
pm2 list
pm2 info kyte-backend
```

### 4. Если используется мок-режим, найти код в логах

```bash
# Поиск кода в логах
pm2 logs kyte-backend --lines 500 --nostream | grep -A 2 -B 2 "MOCK SMS\|код подтверждения"
```

### 5. Проверить установлен ли aws-sdk

```bash
# Проверить package.json
cat package.json | grep aws-sdk

# Или проверить node_modules
ls node_modules | grep aws-sdk
```

### 6. Если aws-sdk не установлен, установить

```bash
npm install aws-sdk
pm2 restart kyte-backend
```

### 7. Проверить переменные окружения в процессе

```bash
# Проверить какие переменные видит процесс
pm2 env kyte-backend | grep -E "SMS|AWS"
```

## Тестирование верификации кода

После получения кода из логов (если мок-режим) или с телефона (если AWS SNS):

```bash
curl -X POST http://94.131.88.135/api/auth/phone/verify-code \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+79686288842",
    "code": "КОД_ИЗ_ЛОГОВ_ИЛИ_С_ТЕЛЕФОНА",
    "name": "Тестовый Пользователь"
  }'
```

## Возможные результаты

### Если мок-режим:
В логах будет:
```
📱 [MOCK SMS] Отправка на +79686288842: Ваш код подтверждения: 123456. Не сообщайте его никому.
```

### Если AWS SNS:
В логах будет:
```
✅ SMS отправлено через AWS SNS. MessageId: ..., Phone: +79686288842
```

### Если ошибка AWS:
В логах будет:
```
❌ Ошибка отправки SMS через AWS SNS: ...
```



