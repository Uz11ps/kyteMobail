# Отладка приложения в браузере

## Быстрый запуск

### Вариант 1: Через web-server (рекомендуется)
```powershell
.\scripts\run_web.ps1
```
Или напрямую:
```bash
flutter run -d web-server --web-port=8080
```
После запуска откройте браузер: http://localhost:8080

### Вариант 2: Прямой запуск Chrome
```powershell
.\scripts\run_chrome.ps1
```
Или напрямую:
```bash
flutter run -d chrome
```

## Отладка сетевых запросов

### 1. Откройте DevTools
- Нажмите `F12` или `Ctrl+Shift+I` в Chrome
- Перейдите на вкладку **Network** (Сеть)

### 2. Фильтрация запросов
- В поле фильтра введите `api` чтобы видеть только API запросы
- Или используйте фильтр `XHR` для AJAX запросов

### 3. Проверка запросов
- **Регистрация**: Должен быть `POST http://94.131.80.213:3000/api/auth/register`
- **Авторизация**: Должен быть `POST http://94.131.80.213:3000/api/auth/login`

### 4. Просмотр деталей запроса
- Кликните на запрос в списке
- Вкладка **Headers**: Проверьте URL, метод, заголовки
- Вкладка **Payload**: Проверьте отправляемые данные
- Вкладка **Response**: Проверьте ответ сервера

## Отладка в консоли Flutter

В терминале где запущено приложение будут видны логи:
- `🌐 API Request:` - отправляемые запросы
- `✅ API Response:` - успешные ответы
- `❌ API Error:` - ошибки

## Типичные проблемы

### Запросы не отправляются
1. Проверьте, что backend запущен: `curl http://94.131.80.213:3000/api/health`
2. Проверьте CORS настройки на backend
3. Проверьте URL в `lib/core/config/app_config.dart`

### Ошибка CORS
Если видите ошибку CORS в консоли браузера:
- Backend должен разрешать запросы с `http://localhost:XXXX` (где XXXX - порт Flutter web)
- Проверьте `CORS_ORIGIN` в `.env` файле backend

### Таймауты
Если запросы долго грузятся:
- Проверьте подключение к интернету
- Проверьте доступность сервера: `ping 94.131.80.213`
- Уменьшите таймауты в `lib/core/network/api_client.dart` (уже уменьшены до 10 сек)

## Проверка ответа сервера

В DevTools → Network → Response вы должны видеть:

**Успешная регистрация:**
```json
{
  "user": {
    "id": "...",
    "email": "...",
    "name": "..."
  },
  "accessToken": "...",
  "refreshToken": "..."
}
```

**Ошибка:**
```json
{
  "message": "Email уже используется"
}
```

## Советы

1. **Очистите кеш браузера** если видите старые данные
2. **Используйте режим инкогнито** для чистого тестирования
3. **Проверяйте консоль браузера** (Console в DevTools) для JavaScript ошибок
4. **Проверяйте терминал Flutter** для Dart логов

