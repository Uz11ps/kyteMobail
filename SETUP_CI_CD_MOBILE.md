# Настройка CI/CD для Flutter веб-приложения

## ✅ Workflow создан!

Файл `.github/workflows/deploy-mobile-web.yml` создан и готов к использованию.

## 📋 Что нужно сделать:

### 1. Настройте Secrets в GitHub

Перейдите в ваш GitHub репозиторий:
1. **Settings** → **Secrets and variables** → **Actions**
2. Добавьте следующие secrets (если их еще нет):

#### SSH_PRIVATE_KEY
- **Name:** `SSH_PRIVATE_KEY`
- **Value:** Содержимое вашего приватного SSH ключа для сервера
  - Откройте файл ключа (например, `C:\Users\1\.ssh\yandex_cloud`)
  - Скопируйте весь текст включая `-----BEGIN` и `-----END`

#### SERVER_USER
- **Name:** `SERVER_USER`
- **Value:** `kyte-777`

#### SERVER_HOST
- **Name:** `SERVER_HOST`
- **Value:** `94.131.80.213`

### 2. Убедитесь что публичный ключ добавлен на сервер

Подключитесь к серверу и проверьте:

```bash
ssh kyte-777@94.131.80.213
cat ~/.ssh/authorized_keys
```

Если вашего ключа нет, добавьте его:

```bash
# На вашем компьютере покажите публичный ключ
cat C:\Users\1\.ssh\yandex_cloud.pub

# На сервере добавьте его
echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

### 3. Создайте директорию на сервере (один раз)

```bash
ssh kyte-777@94.131.80.213
sudo mkdir -p /var/www/kyte-mobile/web
sudo chown -R kyte-777:kyte-777 /var/www/kyte-mobile
```

## 🚀 Как это работает:

### Автоматический запуск:
- При каждом push в `main`/`master` с изменениями в:
  - `lib/**` (код приложения)
  - `web/**` (веб-конфигурация)
  - `pubspec.yaml` (зависимости)

### Ручной запуск:
1. Перейдите в **Actions** в GitHub
2. Выберите **Deploy Flutter Web App to Yandex Cloud**
3. Нажмите **Run workflow**

## ✅ Что происходит при деплое:

1. ✅ Устанавливается Flutter SDK
2. ✅ Устанавливаются зависимости (`flutter pub get`)
3. ✅ Собирается веб-приложение (`flutter build web --release`)
4. ✅ Файлы загружаются на сервер в `/var/www/kyte-mobile/web/`
5. ✅ Настраивается Nginx для раздачи по пути `/mobail`
6. ✅ Обновляется CORS_ORIGIN
7. ✅ Перезапускается backend

## 🌐 Результат:

После успешного деплоя приложение будет доступно:
- **http://94.131.80.213/mobail/**

## 📊 Мониторинг:

Проверяйте статус деплоя в разделе **Actions** вашего GitHub репозитория.

## 🔧 Если что-то пошло не так:

1. Проверьте логи в **Actions** → выберите workflow → выберите job
2. Убедитесь что все secrets настроены правильно
3. Проверьте что публичный ключ добавлен на сервер
4. Проверьте права доступа к директории `/var/www/kyte-mobile/web/`

