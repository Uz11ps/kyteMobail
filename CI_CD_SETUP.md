# Настройка CI/CD для автоматического деплоя

## 🎯 Что это дает:

- ✅ Автоматический деплой при коммитах в `main`/`master`
- ✅ Деплой только при изменении файлов в `backend/`
- ✅ Автоматический backup перед обновлением
- ✅ Проверка статуса после деплоя
- ✅ Возможность запуска вручную

---

## 📋 Шаг 1: Подготовка SSH ключа

### Вариант A: Использовать существующий ключ

Если у вас уже есть SSH ключ для сервера:

1. Скопируйте приватный ключ:
   ```powershell
   # На Windows
   Get-Content C:\Users\1\.ssh\yandex_key\ssh-key-1765132631789
   ```

2. Скопируйте весь вывод (включая `-----BEGIN ...` и `-----END ...`)

### Вариант B: Создать новый ключ специально для CI/CD

```bash
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_deploy_key
```

**Важно:** Не используйте пароль для ключа (просто нажмите Enter)

---

## 📋 Шаг 2: Добавить публичный ключ на сервер

### На сервере:

```bash
ssh -i "C:\Users\1\.ssh\yandex_key\ssh-key-1765132631789" kyte-777@94.131.80.213

# Создайте директорию .ssh если её нет
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавьте публичный ключ
echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

**Или** если используете существующий ключ, он уже должен быть добавлен.

---

## 📋 Шаг 3: Настроить GitHub Secrets

1. Откройте ваш репозиторий на GitHub
2. Перейдите: **Settings → Secrets and variables → Actions**
3. Нажмите **New repository secret**
4. Добавьте следующие секреты:

### `SSH_PRIVATE_KEY`
- **Значение:** Весь приватный ключ (включая `-----BEGIN ...` и `-----END ...`)
- **Важно:** Скопируйте весь ключ, включая первую и последнюю строки

### `SERVER_USER`
- **Значение:** `kyte-777`

### `SERVER_HOST`
- **Значение:** `94.131.80.213`

### `SERVER_PATH`
- **Значение:** `/var/www/kyte-backend/backend`

---

## 📋 Шаг 4: Проверить workflow файл

Убедитесь что файл `.github/workflows/deploy-backend.yml` существует и содержит правильную конфигурацию.

---

## 📋 Шаг 5: Тестирование

### Тест 1: Push в main ветку

```bash
git add .
git commit -m "test: CI/CD deployment"
git push origin main
```

### Тест 2: Запуск вручную

1. Откройте GitHub репозиторий
2. Перейдите: **Actions**
3. Выберите workflow **Deploy Backend to Yandex Cloud**
4. Нажмите **Run workflow**
5. Выберите ветку и нажмите **Run workflow**

---

## 🔍 Мониторинг деплоя

### В GitHub:

1. Откройте **Actions** в репозитории
2. Выберите последний запуск workflow
3. Просмотрите логи каждого шага

### На сервере:

```bash
# Проверьте статус
sudo pm2 status kyte-backend

# Просмотрите логи
sudo pm2 logs kyte-backend --lines 50

# Проверьте последние деплои
ls -la /tmp/kyte-deploy-*
```

---

## 🛠️ Локальный деплой (без CI/CD)

Если нужно задеплоить вручную:

```bash
cd backend
chmod +x deploy.sh
./deploy.sh
```

Или через SSH:

```bash
# С Windows
scp -i "C:\Users\1\.ssh\yandex_key\ssh-key-1765132631789" -r backend kyte-777@94.131.80.213:/tmp/

# На сервере
ssh -i "C:\Users\1\.ssh\yandex_key\ssh-key-1765132631789" kyte-777@94.131.80.213
cd /tmp/backend
chmod +x deploy.sh
./deploy.sh
```

---

## ⚙️ Настройка workflow

### Изменить ветку для деплоя:

Отредактируйте `.github/workflows/deploy-backend.yml`:

```yaml
on:
  push:
    branches:
      - main      # Измените на нужную ветку
      - develop   # Или добавьте другие ветки
```

### Деплой только при изменении определенных файлов:

Уже настроено - деплой только при изменении файлов в `backend/`:

```yaml
paths:
  - 'backend/**'
```

### Добавить уведомления:

Добавьте в workflow после успешного деплоя:

```yaml
- name: Send notification
  if: success()
  run: |
    # Отправка уведомления (Telegram, Slack, Email и т.д.)
```

---

## 🐛 Решение проблем

### Ошибка: Permission denied (publickey)

1. Проверьте что SSH ключ добавлен в GitHub Secrets
2. Проверьте что публичный ключ добавлен на сервере
3. Проверьте права доступа: `chmod 600 ~/.ssh/authorized_keys`

### Ошибка: npm install failed

1. Проверьте что Node.js установлен на сервере
2. Проверьте что есть доступ к npm registry

### Ошибка: PM2 command not found

1. Установите PM2: `sudo npm install -g pm2`
2. Проверьте PATH: `which pm2`

### Деплой не запускается

1. Проверьте что файл workflow находится в `.github/workflows/`
2. Проверьте синтаксис YAML файла
3. Проверьте что секреты добавлены правильно

---

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [PM2 Documentation](https://pm2.keymetrics.io/docs/)

---

## ✅ Готово!

После настройки каждый коммит в `main` ветку будет автоматически деплоить backend на сервер!

