# 🚀 Быстрая настройка CI/CD

## За 5 минут:

### 1. Получите SSH приватный ключ

```powershell
# На Windows
Get-Content C:\Users\1\.ssh\yandex_key\ssh-key-1765132631789
```

Скопируйте весь вывод (включая `-----BEGIN ...` и `-----END ...`)

---

### 2. Добавьте секреты в GitHub

1. Откройте репозиторий: **Settings → Secrets and variables → Actions**
2. Нажмите **New repository secret**

Добавьте 4 секрета:

| Имя | Значение |
|-----|----------|
| `SSH_PRIVATE_KEY` | Весь приватный ключ (из шага 1) |
| `SERVER_USER` | `kyte-777` |
| `SERVER_HOST` | `94.131.88.135` |
| `SERVER_PATH` | `/var/www/kyte-backend/backend` |

---

### 3. Проверьте что публичный ключ на сервере

```bash
ssh -i "C:\Users\1\.ssh\yandex_key\ssh-key-1765132631789" kyte-777@94.131.88.135 "cat ~/.ssh/authorized_keys"
```

Если ключ уже есть - всё готово!

---

### 4. Сделайте коммит

```bash
git add .
git commit -m "feat: add CI/CD for automatic deployment"
git push origin main
```

---

### 5. Проверьте деплой

1. Откройте GitHub: **Actions**
2. Выберите workflow **Deploy Backend to Yandex Cloud**
3. Дождитесь завершения (зеленая галочка ✅)

---

## ✅ Готово!

Теперь каждый коммит в `main` автоматически деплоит backend!

---

## 🔍 Проверка на сервере:

```bash
ssh -i "C:\Users\1\.ssh\yandex_key\ssh-key-1765132631789" kyte-777@94.131.88.135
sudo pm2 status kyte-backend
sudo pm2 logs kyte-backend --lines 20
```

---

## 📚 Подробная инструкция:

См. `CI_CD_SETUP.md` для детальной настройки и решения проблем.

